name: Test custom version
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    env:
      GITHUB_REF: refs/heads/main
      GITHUB_EVENT_NAME: push
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - id: action
        uses: .
        with:
          version: "0.20.0"
      - name: Assert mint is in PATH
        run: |
          echo "Checking mint is available in PATH after installing version 0.20.0..."
          if ! command -v mint > /dev/null 2>&1; then
            echo "FAIL: mint command not found in PATH"
            exit 1
          fi
          echo "PASS: mint is available at $(command -v mint)"
      - name: Assert mint binary is executable
        run: |
          MINT_BIN=$(command -v mint)
          echo "mint binary: $MINT_BIN"
          if [ ! -x "$MINT_BIN" ]; then
            echo "FAIL: mint binary is not executable"
            exit 1
          fi
          echo "PASS: mint binary is executable"
      - name: Assert mint directory exists in workspace
        run: |
          echo "Checking mint directory exists in GITHUB_WORKSPACE..."
          if [ ! -d "${GITHUB_WORKSPACE}/mint" ]; then
            echo "FAIL: expected mint directory at ${GITHUB_WORKSPACE}/mint"
            exit 1
          fi
          echo "PASS: mint directory exists"
